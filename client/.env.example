# Vite exposes env vars prefixed with VITE_
# For local dev, the Vite config proxies /api and /webhook to http://localhost:3000
# so you can leave this empty.
#
# For Vercel (production), set this to your hosted API base URL, e.g.
# VITE_API_BASE_URL=https://your-api.example.com
VITE_API_BASE_URL=
